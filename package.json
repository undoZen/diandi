{
  "name": "diandi",
  "private": true,
  "workspaces": ["web", "mobile", "shared"],
  "scripts": {
    "web:dev": "npm run dev --workspace web",
    "web:build": "npm run build --workspace web",
    "mobile:start": "npm run start --workspace mobile",
    "mobile:bundle:dev": "npm run bundle:dev --workspace mobile",
    "mobile:bundle:release": "npm run bundle:release --workspace mobile"
  },
  "dependencies": {
    "react-native": "0.75.5"
  }
}
